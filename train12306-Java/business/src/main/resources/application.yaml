spring:
  application:
    name: business
  datasource:
    url: jdbc:mysql://localhost:3306/train_business?characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: xiaoqian666
    driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: 8002
  servlet:
    context-path: /business # 路由前缀，方便后期网关管理路由
logging:
  level:
    com.xiaoqian: debug
seata:
  tx-service-group: train-group # 事务组名称
  service:
    vgroup-mapping:
      train-group: default # 事务组和seata集群做关联
    grouplist:
      default: localhost:8091 # seata集群对应的机器
