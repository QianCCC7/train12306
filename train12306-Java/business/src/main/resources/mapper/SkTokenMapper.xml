<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoqian.business.mapper.SkTokenMapper">

    <update id="decrease">
        update sk_token
        set `count` = if (`count` &lt; #{count}, 0, `count` - #{count})
        where `date` = #{date}
          and train_code = #{trainCode}
          and `count` > #{count}
    </update>
</mapper>
